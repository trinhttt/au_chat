import 'package:flutter/material.dart';

class APIConfig {
  static const baseURL = 'https://au-chat-server.herokuapp.com';
  static const baseURLRouteLogin = '/user/login';
//static const themeColor1 = Color.fromRGBO(0, 0, 0, 0.5);//new Color(0xff18191a).withOpacity(0.5);
}
