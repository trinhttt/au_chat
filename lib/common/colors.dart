import 'package:flutter/material.dart';

class AUColors {
  static const bgColor = Color(0xff242526);
  static const themeColor = Color(0xff18191a);
  static const textColor = Colors.white;
  //static const themeColor1 = Color.fromRGBO(0, 0, 0, 0.5);//new Color(0xff18191a).withOpacity(0.5);
}